interface:
  display_name: "Tailwind Patterns"
  short_description: "Tailwind CSS v4 principles. CSS-first configuration, containe..."
  default_prompt: "Use $tailwind-patterns for the current task in this repository."
policy:
  allow_implicit_invocation: true
