interface:
  display_name: "App Builder"
  short_description: "Main application building orchestrator. Creates full-stack ap..."
  default_prompt: "Use $app-builder for the current task in this repository."
policy:
  allow_implicit_invocation: true
