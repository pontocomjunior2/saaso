interface:
  display_name: "Workflow Plan"
  short_description: "Antigravity workflow adapted for Codex. Create project plan u..."
  default_prompt: "Use $workflow-plan for the current task in this repository."
policy:
  allow_implicit_invocation: true
