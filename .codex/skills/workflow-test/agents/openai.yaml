interface:
  display_name: "Workflow Test"
  short_description: "Antigravity workflow adapted for Codex. Test generation and t..."
  default_prompt: "Use $workflow-test for the current task in this repository."
policy:
  allow_implicit_invocation: true
