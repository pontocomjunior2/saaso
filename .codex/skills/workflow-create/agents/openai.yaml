interface:
  display_name: "Workflow Create"
  short_description: "Antigravity workflow adapted for Codex. Create new applicatio..."
  default_prompt: "Use $workflow-create for the current task in this repository."
policy:
  allow_implicit_invocation: true
