interface:
  display_name: "Tdd Workflow"
  short_description: "Test-Driven Development workflow principles. RED-GREEN-REFACT..."
  default_prompt: "Use $tdd-workflow for the current task in this repository."
policy:
  allow_implicit_invocation: true
