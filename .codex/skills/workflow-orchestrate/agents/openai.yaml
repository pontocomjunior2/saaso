interface:
  display_name: "Workflow Orchestrate"
  short_description: "Antigravity workflow adapted for Codex. Coordinate multiple a..."
  default_prompt: "Use $workflow-orchestrate for the current task in this repository."
policy:
  allow_implicit_invocation: true
