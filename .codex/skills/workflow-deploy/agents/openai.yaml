interface:
  display_name: "Workflow Deploy"
  short_description: "Antigravity workflow adapted for Codex. Deployment command fo..."
  default_prompt: "Use $workflow-deploy for the current task in this repository."
policy:
  allow_implicit_invocation: true
