interface:
  display_name: "Testing Patterns"
  short_description: "Testing patterns and principles. Unit, integration, mocking s..."
  default_prompt: "Use $testing-patterns for the current task in this repository."
policy:
  allow_implicit_invocation: true
