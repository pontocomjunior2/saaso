interface:
  display_name: "Powershell Windows"
  short_description: "PowerShell Windows patterns. Critical pitfalls, operator synt..."
  default_prompt: "Use $powershell-windows for the current task in this repository."
policy:
  allow_implicit_invocation: true
