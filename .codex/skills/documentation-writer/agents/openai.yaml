interface:
  display_name: "Documentation Writer"
  short_description: "Expert in technical documentation. Use ONLY when user explici..."
  default_prompt: "Use $documentation-writer for the current task in this repository."
policy:
  allow_implicit_invocation: true
