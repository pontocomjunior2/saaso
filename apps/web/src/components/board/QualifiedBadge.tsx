'use client';

export function QualifiedBadge() {
  return (
    <span
      role="status"
      aria-label="Lead pronto para avançar"
      className="inline-flex items-center rounded-full bg-[#22bd73] px-2 py-0.5 text-xs font-semibold text-white"
    >
      Qualificado
    </span>
  );
}
