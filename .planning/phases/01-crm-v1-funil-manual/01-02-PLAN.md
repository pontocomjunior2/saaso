---
phase: 01-crm-v1-funil-manual
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - apps/api/src/pipeline/pipeline-templates.ts
  - apps/api/src/pipeline/pipeline.service.ts
  - apps/api/src/pipeline/pipeline.controller.ts
  - apps/api/src/pipeline/dto/create-pipeline-from-template.dto.ts
  - apps/api/src/email/email.module.ts
  - apps/api/src/email/email.service.ts
  - apps/api/src/card/card.service.ts
  - apps/api/src/card/card.controller.ts
  - apps/api/src/card/dto/send-message.dto.ts
  - apps/api/package.json
autonomous: true
requirements: [REQ-01, REQ-03]
user_setup:
  - service: mailtrap
    why: "Envio de email transacional via SMTP"
    env_vars:
      - name: MAIL_HOST
        source: "Mailtrap Dashboard -> Inboxes -> SMTP Settings -> Host"
      - name: MAIL_PORT
        source: "Mailtrap Dashboard -> Inboxes -> SMTP Settings -> Port (2525)"
      - name: MAIL_USER
        source: "Mailtrap Dashboard -> Inboxes -> SMTP Settings -> Username"
      - name: MAIL_PASS
        source: "Mailtrap Dashboard -> Inboxes -> SMTP Settings -> Password"
      - name: MAIL_FROM
        source: "Email remetente (ex: noreply@saaso.app)"

must_haves:
  truths:
    - "Pipeline pode ser criado a partir de 1 dos 5 templates via API"
    - "Operador pode enviar mensagem WhatsApp ou Email pelo card com 1 chamada de API"
    - "CardActivity registra canal, template usado e operador que enviou"
  artifacts:
    - path: "apps/api/src/pipeline/pipeline-templates.ts"
      provides: "5 templates de pipeline em JSON estatico"
      exports: ["PIPELINE_TEMPLATES"]
    - path: "apps/api/src/email/email.service.ts"
      provides: "Envio de email via nodemailer + SMTP"
      exports: ["EmailService"]
    - path: "apps/api/src/card/dto/send-message.dto.ts"
      provides: "DTO para POST /cards/:id/send-message"
      exports: ["SendMessageDto"]
  key_links:
    - from: "apps/api/src/card/card.service.ts"
      to: "apps/api/src/whatsapp/whatsapp.service.ts"
      via: "injection + logMessage()"
      pattern: "whatsappService\\.logMessage"
    - from: "apps/api/src/card/card.service.ts"
      to: "apps/api/src/email/email.service.ts"
      via: "injection + sendEmail()"
      pattern: "emailService\\.sendEmail"
    - from: "apps/api/src/pipeline/pipeline.service.ts"
      to: "apps/api/src/pipeline/pipeline-templates.ts"
      via: "import PIPELINE_TEMPLATES"
      pattern: "PIPELINE_TEMPLATES"
---

<objective>
Implementar 3 features backend: (1) criacao de pipeline a partir de template, (2) EmailService com nodemailer/Mailtrap, (3) endpoint POST /cards/:id/send-message que dispara WhatsApp ou Email usando template da etapa.

Purpose: Completa toda a API necessaria para o fluxo manual de CRM — o frontend so precisa consumir estes endpoints.
Output: 3 endpoints novos funcionais, EmailService operacional.
</objective>

<execution_context>
@.planning/phases/01-crm-v1-funil-manual/01-RESEARCH.md
</execution_context>

<context>
@.planning/ROADMAP.md
@apps/api/src/pipeline/pipeline.service.ts
@apps/api/src/pipeline/pipeline.controller.ts
@apps/api/src/card/card.controller.ts
@apps/api/src/card/card.service.ts
@apps/api/src/whatsapp/whatsapp.service.ts
@apps/api/src/common/decorators/current-user.decorator.ts
@.planning/notes/pipeline-templates.md
@.planning/phases/01-crm-v1-funil-manual/01-01-SUMMARY.md

<interfaces>
De apps/api/src/whatsapp/whatsapp.service.ts:
```typescript
// logMessage e o metodo que envia WhatsApp e cria CardActivity
// Aceita: tenantId, { contactId, content, direction?, cardId? }
export class WhatsappService {
  async logMessage(tenantId: string, createMessageDto: CreateMessageDto): Promise<WhatsAppMessage>
}
```

De apps/api/src/card/card.controller.ts:
```typescript
@UseGuards(JwtAuthGuard, TenantGuard)
@Controller('cards')
export class CardController {
  // Padrao: @CurrentTenant() tenantId: string
}
```

De apps/api/src/pipeline/pipeline.service.ts:
```typescript
export class PipelineService {
  public async create(tenantId: string, dto: CreatePipelineDto): Promise<Pipeline>
  // Usa prisma.pipeline.create
}
```

Modelo StageMessageTemplate (criado no Plan 01):
```prisma
model StageMessageTemplate {
  id, stageId, name, channel (CampaignChannel), subject?, body, tenantId, createdAt, updatedAt
}
```

Modelo CardActivity expandido (criado no Plan 01):
```prisma
model CardActivity {
  id, cardId, type, content, channel?, templateName?, actorId?, createdAt
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pipeline templates estaticos + endpoint POST /pipelines/from-template</name>
  <files>
    apps/api/src/pipeline/pipeline-templates.ts,
    apps/api/src/pipeline/pipeline.service.ts,
    apps/api/src/pipeline/pipeline.controller.ts,
    apps/api/src/pipeline/dto/create-pipeline-from-template.dto.ts
  </files>
  <action>
1. Criar `apps/api/src/pipeline/pipeline-templates.ts` com os 5 templates baseados em `.planning/notes/pipeline-templates.md`. Cada template tem:
```typescript
export interface PipelineTemplate {
  id: string;
  name: string;
  description: string;
  stages: Array<{
    name: string;
    order: number;
    messageTemplates: Array<{
      name: string;
      channel: 'WHATSAPP' | 'EMAIL';
      subject?: string;
      body: string;
    }>;
  }>;
}

export const PIPELINE_TEMPLATES: PipelineTemplate[] = [
  {
    id: 'funil-simples',
    name: 'Funil Simples',
    description: 'Negocios diretos, prestadores de servico, pequenos times',
    stages: [
      {
        name: 'Novo Lead', order: 1,
        messageTemplates: [
          { name: 'Boas-vindas', channel: 'WHATSAPP', body: 'Ola {{nome}}! Recebemos seu contato e estamos prontos para ajudar. Como posso te atender hoje?' },
          { name: 'Follow-up D+1', channel: 'WHATSAPP', body: 'Oi {{nome}}, tudo bem? Queria saber se voce conseguiu avaliar nossa proposta. Estou a disposicao!' },
          { name: 'Follow-up D+3', channel: 'WHATSAPP', body: '{{nome}}, estou passando aqui para ver se posso ajudar com alguma duvida. Nossos clientes costumam ter resultados rapidos!' },
        ],
      },
      // ... demais etapas conforme pipeline-templates.md
    ],
  },
  // ... templates 2-5 (funil-comercial-medio, funil-saas-completo, funil-ecommerce, pos-venda-cs)
];
```

Incluir templates de mensagem para TODAS as etapas conforme documentado em `pipeline-templates.md`. As variaveis sao: `{{nome}}`, `{{empresa}}`, `{{telefone}}`, `{{email}}`.

2. Criar DTO `create-pipeline-from-template.dto.ts`:
```typescript
export class CreatePipelineFromTemplateDto {
  @IsString() @IsNotEmpty() templateId: string;
  @IsString() @IsOptional() name?: string; // override do nome
}
```

3. Adicionar metodo ao `PipelineService`:
```typescript
public async createFromTemplate(tenantId: string, dto: CreatePipelineFromTemplateDto): Promise<Pipeline> {
  const template = PIPELINE_TEMPLATES.find(t => t.id === dto.templateId);
  if (!template) throw new NotFoundException(`Template '${dto.templateId}' nao encontrado.`);

  return this.prisma.$transaction(async (tx) => {
    const pipeline = await tx.pipeline.create({
      data: {
        name: dto.name || template.name,
        tenantId,
      },
    });

    for (const stageDef of template.stages) {
      const stage = await tx.stage.create({
        data: {
          name: stageDef.name,
          order: stageDef.order,
          pipelineId: pipeline.id,
        },
      });

      if (stageDef.messageTemplates.length > 0) {
        await tx.stageMessageTemplate.createMany({
          data: stageDef.messageTemplates.map((mt) => ({
            stageId: stage.id,
            name: mt.name,
            channel: mt.channel,
            subject: mt.subject ?? null,
            body: mt.body,
            tenantId,
          })),
        });
      }
    }

    return pipeline;
  });
}
```

4. Adicionar 2 endpoints ao `PipelineController`:
```typescript
@Get('templates')
public async listTemplates() {
  // Retorna lista de templates (sem autenticacao necessaria, sao publicos)
  return PIPELINE_TEMPLATES.map(t => ({
    id: t.id,
    name: t.name,
    description: t.description,
    stagesCount: t.stages.length,
    stages: t.stages.map(s => ({ name: s.name, order: s.order })),
  }));
}

@Post('from-template')
public async createFromTemplate(
  @CurrentTenant() tenantId: string,
  @Body() dto: CreatePipelineFromTemplateDto,
) {
  return this.pipelineService.createFromTemplate(tenantId, dto);
}
```

IMPORTANTE: O endpoint `GET /pipelines/templates` DEVE ser registrado ANTES de `GET /pipelines/:id` no controller, caso contrario o NestJS vai interpretar "templates" como um ID. Reordenar os metodos no controller se necessario, ou usar `@Get('templates')` com rota fixa.

5. Atualizar o `findAll` e `findOne` do PipelineService para incluir `messageTemplates` no include de stages:
```typescript
stages: {
  orderBy: { order: 'asc' },
  include: {
    // ... includes existentes ...
    messageTemplates: {
      orderBy: { createdAt: 'asc' },
      select: { id: true, name: true, channel: true, subject: true, body: true },
    },
  },
}
```
  </action>
  <verify>
    <automated>cd apps/api && npx nest build 2>&1 | tail -5</automated>
  </verify>
  <done>
- `GET /pipelines/templates` retorna 5 templates com etapas e descricao
- `POST /pipelines/from-template` cria pipeline + stages + messageTemplates em transacao
- `GET /pipelines` e `GET /pipelines/:id` incluem messageTemplates por stage
  </done>
</task>

<task type="auto">
  <name>Task 2: EmailService + endpoint POST /cards/:id/send-message</name>
  <files>
    apps/api/src/email/email.module.ts,
    apps/api/src/email/email.service.ts,
    apps/api/src/card/card.service.ts,
    apps/api/src/card/card.controller.ts,
    apps/api/src/card/dto/send-message.dto.ts,
    apps/api/src/card/card.module.ts,
    apps/api/src/app.module.ts,
    apps/api/package.json
  </files>
  <action>
1. Instalar nodemailer:
```bash
cd apps/api && npm install nodemailer && npm install -D @types/nodemailer
```

2. Criar `apps/api/src/email/email.service.ts`:
```typescript
import { Injectable, Logger } from '@nestjs/common';
import * as nodemailer from 'nodemailer';

@Injectable()
export class EmailService {
  private readonly logger = new Logger(EmailService.name);
  private transporter: nodemailer.Transporter;

  constructor() {
    const host = process.env.MAIL_HOST;
    const port = Number(process.env.MAIL_PORT || '2525');
    const user = process.env.MAIL_USER;
    const pass = process.env.MAIL_PASS;

    if (host && user && pass) {
      this.transporter = nodemailer.createTransport({
        host, port, auth: { user, pass },
      });
      this.logger.log(`Email transporter configurado: ${host}:${port}`);
    } else {
      this.logger.warn('Variaveis MAIL_HOST/MAIL_USER/MAIL_PASS ausentes — email em modo local_demo (nao envia de verdade).');
    }
  }

  async sendEmail(params: {
    to: string;
    subject: string;
    body: string;
  }): Promise<{ success: boolean; deliveryMode: 'smtp' | 'local_demo'; messageId?: string }> {
    if (!this.transporter) {
      this.logger.log(`[local_demo] Email simulado para ${params.to}: ${params.subject}`);
      return { success: true, deliveryMode: 'local_demo' };
    }

    const info = await this.transporter.sendMail({
      from: process.env.MAIL_FROM || 'noreply@saaso.app',
      to: params.to,
      subject: params.subject,
      text: params.body,
    });

    this.logger.log(`Email enviado para ${params.to}, messageId: ${info.messageId}`);
    return { success: true, deliveryMode: 'smtp', messageId: info.messageId };
  }
}
```

3. Criar `apps/api/src/email/email.module.ts`:
```typescript
@Module({ providers: [EmailService], exports: [EmailService] })
export class EmailModule {}
```

4. Registrar EmailModule no AppModule.

5. Criar `apps/api/src/card/dto/send-message.dto.ts`:
```typescript
export class SendMessageDto {
  @IsString() @IsNotEmpty() templateId: string;
  @IsEnum(CampaignChannel) channel: CampaignChannel;
}
```

6. Adicionar metodo `sendMessage` ao `CardService`:

```typescript
async sendMessage(
  tenantId: string,
  cardId: string,
  userId: string,
  dto: SendMessageDto,
): Promise<{ success: boolean; deliveryMode: string }> {
  // 1. Buscar card com contact e stage
  const card = await this.prisma.card.findFirst({
    where: { id: cardId, tenantId },
    include: {
      contact: true,
      stage: true,
    },
  });
  if (!card) throw new NotFoundException('Card nao encontrado.');
  if (!card.contact) throw new BadRequestException('Card nao tem contato vinculado.');

  // 2. Buscar template
  const template = await this.prisma.stageMessageTemplate.findFirst({
    where: { id: dto.templateId, tenantId },
  });
  if (!template) throw new NotFoundException('Template nao encontrado.');

  // 3. Resolver variaveis no body
  const contact = card.contact;
  let resolvedBody = template.body
    .replace(/\{\{nome\}\}/g, contact.name || '')
    .replace(/\{\{email\}\}/g, contact.email || '')
    .replace(/\{\{telefone\}\}/g, contact.phone || '')
    .replace(/\{\{empresa\}\}/g, ''); // empresa requer join extra — deixar vazio por ora

  let resolvedSubject = template.subject
    ? template.subject.replace(/\{\{nome\}\}/g, contact.name || '')
    : template.name;

  // 4. Despachar por canal
  let deliveryMode = 'unknown';

  if (dto.channel === 'WHATSAPP') {
    if (!contact.phone) throw new BadRequestException('Contato nao tem telefone para WhatsApp.');
    const result = await this.whatsappService.logMessage(tenantId, {
      contactId: contact.id,
      content: resolvedBody,
      cardId: card.id,
    });
    deliveryMode = 'whatsapp';
  } else if (dto.channel === 'EMAIL') {
    if (!contact.email) throw new BadRequestException('Contato nao tem email.');
    const result = await this.emailService.sendEmail({
      to: contact.email,
      subject: resolvedSubject,
      body: resolvedBody,
    });
    deliveryMode = result.deliveryMode;

    // Criar CardActivity para email (WhatsApp ja cria via logMessage)
    await this.prisma.cardActivity.create({
      data: {
        cardId: card.id,
        type: 'EMAIL_OUTBOUND',
        content: `Email enviado para ${contact.email}: "${resolvedSubject}"`,
        channel: 'EMAIL',
        templateName: template.name,
        actorId: userId,
      },
    });
  }

  // 5. Atualizar actorId e templateName no CardActivity de WhatsApp (se foi whatsapp)
  // O logMessage ja criou o CardActivity — precisamos atualizar os novos campos
  if (dto.channel === 'WHATSAPP') {
    const latestActivity = await this.prisma.cardActivity.findFirst({
      where: { cardId: card.id, type: { startsWith: 'WHATSAPP_OUTBOUND' } },
      orderBy: { createdAt: 'desc' },
    });
    if (latestActivity) {
      await this.prisma.cardActivity.update({
        where: { id: latestActivity.id },
        data: { channel: 'WHATSAPP', templateName: template.name, actorId: userId },
      });
    }
  }

  return { success: true, deliveryMode };
}
```

IMPORTANTE: Injetar `WhatsappService` e `EmailService` no `CardService`. Atualizar `CardModule` para importar `WhatsappModule` e `EmailModule`. Verificar se `WhatsappModule` exporta `WhatsappService` — se nao, adicionar `exports: [WhatsappService]`.

7. Adicionar endpoint ao `CardController`:
```typescript
@Post(':id/send-message')
public async sendMessage(
  @CurrentTenant() tenantId: string,
  @CurrentUser() user: { id: string },
  @Param('id') id: string,
  @Body() dto: SendMessageDto,
) {
  return this.cardService.sendMessage(tenantId, id, user.id, dto);
}
```

Importar `CurrentUser` de `../common/decorators/current-user.decorator`.
  </action>
  <verify>
    <automated>cd apps/api && npx nest build 2>&1 | tail -5</automated>
  </verify>
  <done>
- `POST /cards/:id/send-message` com `{ templateId, channel }` envia WhatsApp ou Email
- CardActivity registra channel, templateName e actorId para ambos os canais
- EmailService funciona com Mailtrap (smtp) ou simula em local_demo
- Build NestJS sem erros
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> POST /cards/:id/send-message | Input nao-confiavel — templateId e channel |
| client -> POST /pipelines/from-template | Input nao-confiavel — templateId |
| API -> Mailtrap SMTP | Credenciais de email transitam |
| API -> Meta Cloud API | Credenciais WhatsApp transitam |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | send-message endpoint | mitigate | JwtAuthGuard + TenantGuard + CurrentUser — operador autenticado |
| T-02-02 | Tampering | templateId no send-message | mitigate | Validar que template pertence ao tenant antes de usar |
| T-02-03 | Info Disclosure | email body com variaveis | mitigate | Apenas dados do proprio contact do card sao injetados — sem acesso cross-tenant |
| T-02-04 | Denial of Service | send-message sem rate limit | accept | Fase v1 — volume baixo. Rate limiting sera adicionado em fase posterior |
| T-02-05 | Tampering | pipeline from-template | mitigate | templateId validado contra lista estatica — impossivel injetar template customizado |
| T-02-06 | Info Disclosure | SMTP credentials | mitigate | Credenciais em env vars, nunca expostas na API response |
</threat_model>

<verification>
1. `npx nest build` compila sem erros
2. `GET /pipelines/templates` retorna 5 templates
3. `POST /pipelines/from-template` cria pipeline com etapas e templates de mensagem
4. `POST /cards/:id/send-message` retorna success com deliveryMode
</verification>

<success_criteria>
- [ ] 5 templates de pipeline acessiveis via GET /pipelines/templates
- [ ] POST /pipelines/from-template cria pipeline + stages + StageMessageTemplates em transacao
- [ ] EmailService envia via SMTP ou opera em local_demo sem credenciais
- [ ] POST /cards/:id/send-message envia por WhatsApp ou Email usando template
- [ ] CardActivity registra channel, templateName e actorId em ambos os canais
- [ ] Build NestJS sem erros
</success_criteria>

<output>
After completion, create `.planning/phases/01-crm-v1-funil-manual/01-02-SUMMARY.md`
</output>
