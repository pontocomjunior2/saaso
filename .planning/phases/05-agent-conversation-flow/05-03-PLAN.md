---
phase: 05-agent-conversation-flow
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - apps/api/src/agent/workers/conversation-summarizer.queue.ts
  - apps/api/src/agent/workers/conversation-summarizer.queue.spec.ts
  - apps/api/src/agent/workers/agent-retry.queue.ts
  - apps/api/src/agent/workers/agent-retry.queue.spec.ts
  - apps/api/src/agent/workers/agent-retry.types.ts
autonomous: true
requirements:
  - D-19
  - D-21
tags: [agent, queues, bullmq, redis, retry, summarizer]

must_haves:
  truths:
    - "ConversationSummarizerQueue enqueues summary jobs keyed by conversationId with concurrency 1"
    - "ConversationSummarizerQueue worker calls AiService.generateResponse with a condense-to-500-tokens prompt and overwrites AgentConversation.summary on success"
    - "ConversationSummarizerQueue failure preserves previous summary (D-21) and marks BullMQ job failed"
    - "AgentRetryQueue enqueues with exponential backoff 2s/8s/32s, max 3 attempts (D-19)"
    - "AgentRetryQueue worker re-invokes the runner's processInboundMessage entry with the persisted payload"
    - "AgentRetryQueue emits AGENT_PERSISTENT_FAILURE notification on final attempt fail"
  artifacts:
    - path: apps/api/src/agent/workers/conversation-summarizer.queue.ts
      provides: ConversationSummarizerQueue @Injectable with OnModuleInit/Destroy
      exports: [ConversationSummarizerQueue]
    - path: apps/api/src/agent/workers/agent-retry.queue.ts
      provides: AgentRetryQueue @Injectable with OnModuleInit/Destroy
      exports: [AgentRetryQueue]
    - path: apps/api/src/agent/workers/agent-retry.types.ts
      provides: AgentRetryJobPayload type (shared between enqueue + worker)
  key_links:
    - from: apps/api/src/agent/workers/conversation-summarizer.queue.ts
      to: AgentConversation.summary
      via: prisma.agentConversation.update
      pattern: "agentConversation\\.update"
    - from: apps/api/src/agent/workers/agent-retry.queue.ts
      to: NotificationService.emit
      via: AGENT_PERSISTENT_FAILURE type
      pattern: "AGENT_PERSISTENT_FAILURE"
---

<objective>
Ship the two BullMQ workers that keep the runner hot path clean: a summarizer that rolls long history into `AgentConversation.summary` asynchronously, and a retry queue that re-runs the runner when the OpenAI provider fails transiently. Both mirror the exact pattern of `apps/api/src/stage-rule/stage-rule-queue.service.ts` — no new infra, no new env vars beyond optional driver-preference and concurrency knobs.

Purpose: removes the two remaining asynchronous obligations from the runner (D-19, D-21, D-02) so plan 05-04 can keep the orchestrator as pure synchronous cascade.
Output: two `@Injectable()` queue+worker services with specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-agent-conversation-flow/05-CONTEXT.md
@.planning/phases/05-agent-conversation-flow/05-AI-SPEC.md
@.planning/phases/05-agent-conversation-flow/05-RESEARCH.md
@.planning/phases/05-agent-conversation-flow/05-PATTERNS.md

<interfaces>
From apps/api/src/stage-rule/stage-rule-queue.service.ts (EXACT template to mirror — read lines 1-200):
- imports: `Injectable, Logger, OnModuleDestroy, OnModuleInit, forwardRef, Inject` + `Queue, Worker` from 'bullmq' + `IORedis, type RedisOptions` from 'ioredis'
- private fields: queueName, queueDriverPreference, redisHost, redisPort, redisDb, redisPassword, workerConcurrency
- onModuleInit: builds Queue + Worker + healthConnection.ping() + waitUntilReady + finally healthConnection.quit() + worker.on('error', ...)
- onModuleDestroy: worker.close() + queue.close() + connection.quit()
- enqueue pattern: `queue.add(jobName, payload, { jobId, delay })`

From apps/api/src/common/services/ai.service.ts (existing, for summarizer):
```typescript
async generateResponse(systemPrompt: string, userMessage: string, options?: GenerateAgentResponseOptions): Promise<string>
```

From apps/api/src/notification/notification.service.ts:
```typescript
emit(tenantId: string, event: NotificationEvent): void
```

From apps/api/src/agent/handlers/structured-reply.generator.ts (plan 05-02):
```typescript
export class AgentProviderError extends Error { constructor(reason: 'provider'|'empty', raw: string|null) }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ConversationSummarizerQueue — BullMQ queue+worker, concurrency 1, overwrites AgentConversation.summary, preserves on failure</name>
  <files>
    apps/api/src/agent/workers/conversation-summarizer.queue.ts,
    apps/api/src/agent/workers/conversation-summarizer.queue.spec.ts
  </files>
  <read_first>
    - apps/api/src/stage-rule/stage-rule-queue.service.ts (full file — the line-for-line template per PATTERNS §workers)
    - apps/api/src/common/services/ai.service.ts (generateResponse signature, existing temperature/maxTokens normalization)
    - .planning/phases/05-agent-conversation-flow/05-CONTEXT.md D-02, D-21
    - .planning/phases/05-agent-conversation-flow/05-AI-SPEC.md §4 State Management: Summarizer worker
    - .planning/phases/05-agent-conversation-flow/05-RESEARCH.md Pitfall #7 (summarizer racing)
  </read_first>
  <behavior>
    - Queue name: `agent_summarize`
    - Worker concurrency: 1 (per RESEARCH Pitfall #7 — global concurrency=1 acceptable at SMB scale)
    - Job payload: `{ conversationId: string }`
    - Job name: `agent_summarize.execute`
    - `enqueue(conversationId, { delayMs?: number })` method uses `jobId: conversationId` so duplicate enqueues dedupe
    - Worker process:
      1. Load last 50 `AgentMessage` rows for the conversation (oldest-first)
      2. If <4 messages → return early (nothing to summarize)
      3. Call `aiService.generateResponse(systemPrompt, userMessage)` where systemPrompt = `"Condense a conversa abaixo em um resumo de no máximo 500 tokens em português brasileiro. Mantenha fatos específicos (orçamento, papel, dor, prazo), use tom neutro, não invente nada que não esteja na conversa."` and userMessage = joined role:content rows
      4. On success AND returned text non-empty → `prisma.agentConversation.update({ where: { id }, data: { summary: text } })`
      5. On error/empty → rethrow (BullMQ marks job failed; previous summary stays untouched — D-21)
    - Driver preference: accept `AGENT_SUMMARIZE_RUNTIME_DRIVER` env; `poller` mode skips BullMQ and no-ops (parity with stage-rule service)
  </behavior>
  <action>
Create `apps/api/src/agent/workers/conversation-summarizer.queue.ts`. Structure copied from `apps/api/src/stage-rule/stage-rule-queue.service.ts` — same imports, same onModuleInit/Destroy skeleton, same healthConnection ping logic. Adapt the queue name, payload shape, and worker process function.

Key deltas from the stage-rule template:
- `queueName = 'agent_summarize'`
- `driverEnvVar = 'AGENT_SUMMARIZE_RUNTIME_DRIVER'`
- `concurrencyEnvVar = 'AGENT_SUMMARIZE_CONCURRENCY'` default 1 (clamped to max 1 — single instance per RESEARCH Pitfall #7 mitigation)
- Worker process signature:
  ```typescript
  private async processJob(job: Job<{ conversationId: string }>): Promise<void> {
    const { conversationId } = job.data;
    const rows = await this.prisma.agentMessage.findMany({
      where: { conversationId },
      orderBy: { createdAt: 'asc' },
      take: 50,
      select: { role: true, content: true, createdAt: true },
    });
    if (rows.length < 4) return;

    const transcript = rows
      .map((m) => `${m.role === 'AGENT' ? 'Agente' : 'Lead'}: ${m.content}`)
      .join('\n');

    const systemPrompt =
      'Condense a conversa abaixo em um resumo de no máximo 500 tokens em português brasileiro. '
      + 'Mantenha fatos específicos (orçamento, papel, dor, prazo), use tom neutro, '
      + 'não invente nada que não esteja na conversa.';

    const summary = await this.aiService.generateResponse(systemPrompt, transcript, {
      temperature: 0.2,
      maxTokens: 600,
    });

    const text = summary?.trim();
    if (!text) {
      // Preserve previous summary; mark job failed by throwing
      throw new Error(`Summarizer returned empty output for conversation ${conversationId}`);
    }

    await this.prisma.agentConversation.update({
      where: { id: conversationId },
      data: { summary: text },
    });
  }
  ```
- `enqueue(conversationId: string, opts?: { delayMs?: number }): Promise<void>` uses `jobId: conversationId, delay: opts?.delayMs ?? 0`
- Constructor injects `PrismaService` + `AiService`. No forwardRef needed (summarizer does not depend on runner).

Register the service in `apps/api/src/agent/agent.module.ts` providers array. Also add it to exports so plan 05-04 can inject it into the runner without another module edit.

Create spec covering (mock PrismaService + AiService, do NOT start real Redis):
1. enqueue: stubs `queue.add` (inject via private setter or expose via a spyable wrapper); verify called with `{conversationId: 'c1'}, { jobId: 'c1', delay: 0 }`
2. processJob early-exit when <4 messages (AiService.generateResponse NOT called, prisma.agentConversation.update NOT called)
3. processJob happy path: 10 messages → generateResponse called with concatenated transcript → prisma.agentConversation.update called with summary text
4. processJob preserves previous summary on empty AI output: AiService returns '' → throws → prisma.agentConversation.update NOT called (D-21)
5. processJob preserves previous summary on AI error: AiService throws → error propagates (BullMQ marks failed) → prisma.agentConversation.update NOT called

Note for the executor: the stage-rule service file lazily builds its `Worker` inside onModuleInit. For unit tests, factor `processJob` as a public-but-`@internal` method so specs can call it directly without booting BullMQ.
  </action>
  <verify>
    <automated>cd apps/api &amp;&amp; pnpm test -- --testPathPattern="agent/workers/conversation-summarizer" --passWithNoTests=false</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "agent_summarize" apps/api/src/agent/workers/conversation-summarizer.queue.ts` returns ≥1
    - `grep -n "concurrency" apps/api/src/agent/workers/conversation-summarizer.queue.ts | grep -E "\b1\b"` returns ≥1 (enforced concurrency 1)
    - `grep -n "jobId:" apps/api/src/agent/workers/conversation-summarizer.queue.ts | grep "conversationId"` returns 1 (dedup)
    - Spec file green with all 5 cases
    - `grep -n "ConversationSummarizerQueue" apps/api/src/agent/agent.module.ts` returns ≥2 (providers + exports)
  </acceptance_criteria>
  <done>Summarizer queue+worker shipped, concurrency 1, summary preserved on failure, module registered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AgentRetryQueue — exponential backoff 2s/8s/32s max 3 attempts + AGENT_PERSISTENT_FAILURE notification on final fail</name>
  <files>
    apps/api/src/agent/workers/agent-retry.types.ts,
    apps/api/src/agent/workers/agent-retry.queue.ts,
    apps/api/src/agent/workers/agent-retry.queue.spec.ts
  </files>
  <read_first>
    - apps/api/src/stage-rule/stage-rule-queue.service.ts (template)
    - apps/api/src/notification/notification.service.ts
    - apps/api/src/agent/handlers/structured-reply.generator.ts (AgentProviderError for runner-side throw)
    - .planning/phases/05-agent-conversation-flow/05-CONTEXT.md D-19
    - .planning/phases/05-agent-conversation-flow/05-AI-SPEC.md §4b Retry table + Guardrails
    - .planning/phases/05-agent-conversation-flow/05-RESEARCH.md Don't Hand-Roll table (retry)
  </read_first>
  <behavior>
    - Queue name: `agent_retry`
    - Worker concurrency: default 1 (env `AGENT_RETRY_CONCURRENCY`)
    - Job name: `agent_retry.execute`
    - BullMQ job options: `{ attempts: 3, backoff: { type: 'exponential', delay: 2000 } }` — yields 2s, 8s, 32s steps (exponential factor 4x)
    - Payload type (exported in agent-retry.types.ts so runner can import without circular dep):
      ```typescript
      export interface AgentRetryJobPayload {
        conversationId: string;
        cardId: string;
        tenantId: string;
        agentId: string;
        contactId: string;
        inboundContent: string;
        whatsAppMessageId: string | null;
        enqueuedAt: string; // ISO
      }
      ```
    - Worker process:
      1. Inject `AgentRunnerService` via `forwardRef(() => AgentRunnerService)` — call `runner.processInboundMessage(payload)` (new signature exposed in plan 05-04)
      2. If `processInboundMessage` throws `AgentProviderError` → rethrow so BullMQ retries
      3. If `processInboundMessage` succeeds → noop (done)
    - On final attempt failed (`job.attemptsMade === job.opts.attempts`):
      - Write `CardActivity` type=AGENT_ERROR with `metadata: { reason: 'persistent_failure', attempts: 3, last_error: message }`
      - `notificationService.emit(tenantId, { type: 'AGENT_PERSISTENT_FAILURE', cardId, cardTitle: '', at: new Date().toISOString() })`
      - Detect finality via the `worker.on('failed', (job, err) => ...)` listener checking `job.attemptsMade >= (job.opts.attempts ?? 1)`
  </behavior>
  <action>
Create `apps/api/src/agent/workers/agent-retry.types.ts` with the `AgentRetryJobPayload` interface above. Placing the type in its own file avoids pulling the queue class (and BullMQ import chain) into the runner module graph.

Create `apps/api/src/agent/workers/agent-retry.queue.ts` mirroring the stage-rule template. Key deltas:
- Constructor injects `PrismaService`, `NotificationService`, and `@Inject(forwardRef(() => AgentRunnerService)) runner: AgentRunnerService` (runner signature introduced in plan 05-04)
- Enqueue method signature:
  ```typescript
  async enqueue(payload: AgentRetryJobPayload): Promise<void> {
    if (!this.queue) return;
    await this.queue.add('agent_retry.execute', payload, {
      attempts: 3,
      backoff: { type: 'exponential', delay: 2000 },
      removeOnComplete: 100,
      removeOnFail: 500,
    });
  }
  ```
- Worker process: `await this.runner.processInboundMessage(job.data)` — if it throws, BullMQ automatically retries according to `attempts`/`backoff`.
- `worker.on('failed', async (job, err) => { ... })` registers the final-attempt handler — write AGENT_ERROR activity + emit AGENT_PERSISTENT_FAILURE when `job && job.attemptsMade >= (job.opts.attempts ?? 1)`.

Register the service in `apps/api/src/agent/agent.module.ts` providers + exports.

Create spec covering (do NOT boot real BullMQ — call the process function + onFailed handler directly):
1. enqueue: wraps `queue.add` with attempts:3 and exponential backoff delay:2000
2. processJob delegates to runner.processInboundMessage with the full payload
3. processJob rethrows runner errors (triggers BullMQ retry)
4. On job failure with `attemptsMade=1` (not final) → NO AGENT_ERROR activity, NO notification
5. On job failure with `attemptsMade=3` (final) → writes AGENT_ERROR activity AND emits AGENT_PERSISTENT_FAILURE notification with tenantId from payload

Note: testing the onFailed handler typically means extracting it as a named private method `private async onJobFailed(job, err)` called by the `worker.on('failed', ...)` listener. The spec invokes the method directly with a stub job object.
  </action>
  <verify>
    <automated>cd apps/api &amp;&amp; pnpm test -- --testPathPattern="agent/workers/agent-retry" --passWithNoTests=false</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/agent/workers/agent-retry.types.ts` exists and exports `AgentRetryJobPayload` interface
    - `grep -n "agent_retry" apps/api/src/agent/workers/agent-retry.queue.ts` returns ≥1
    - `grep -n "attempts: 3" apps/api/src/agent/workers/agent-retry.queue.ts` returns ≥1
    - `grep -n "exponential" apps/api/src/agent/workers/agent-retry.queue.ts` returns ≥1
    - `grep -n "AGENT_PERSISTENT_FAILURE" apps/api/src/agent/workers/agent-retry.queue.ts` returns ≥1
    - `grep -n "forwardRef" apps/api/src/agent/workers/agent-retry.queue.ts` returns ≥1 (circular dep handled per PATTERNS §S4)
    - `grep -n "AgentRetryQueue" apps/api/src/agent/agent.module.ts` returns ≥2
    - Spec file green with all 5 cases
  </acceptance_criteria>
  <done>Retry queue enqueues with correct backoff, delegates to runner, emits persistent-failure on 3rd attempt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| apps/api → Redis | BullMQ job payloads include tenantId, cardId, inbound content |
| apps/api → OpenAI (summarizer path) | Conversation transcript crosses into summarization call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | DoS | Poison pill job re-enqueued forever | mitigate | `attempts: 3` hard ceiling on retry queue; `removeOnFail: 500` bounds dead-letter volume (RESEARCH §security domain — BullMQ job poisoning). |
| T-05-03-02 | Information Disclosure | Redis stores PII (inbound content) | accept | Redis is same-perimeter; BullMQ payloads expire via `removeOnComplete`/`removeOnFail`. No cross-tenant key sharing (jobIds namespaced via conversationId). |
| T-05-03-03 | Tampering | Concurrent summarizer jobs race on same conversation | mitigate | Concurrency 1 globally (acceptable at SMB scale per RESEARCH Pitfall #7); `jobId: conversationId` dedupes rapid re-enqueues in the queue itself. |
| T-05-03-04 | Information Disclosure | Summary sent to OpenAI leaks conversation to external provider | accept | Inherits existing Phase 3 LGPD posture; `store: false` on OpenAI call already set via AiService.generateResponse default; Phoenix anonymization (deferred to future observability plan per Environment Availability table). |
| T-05-03-05 | Repudiation | Retry handler fires AGENT_PERSISTENT_FAILURE twice | mitigate | Handler guards with `job.attemptsMade >= job.opts.attempts` check — fires exactly once on the last attempt per BullMQ semantics. |
</threat_model>

<verification>
- `pnpm --filter @saaso/api test -- --testPathPattern="agent/workers"` all green
- `pnpm --filter @saaso/api exec tsc --noEmit` exits 0
- `grep -n "ConversationSummarizerQueue\|AgentRetryQueue" apps/api/src/agent/agent.module.ts` shows both registered in providers and exports
- Module graph: no direct import of `agent-retry.queue.ts` from runner — runner imports `agent-retry.types.ts` only (avoids circular dep); queue imports runner via `forwardRef`
</verification>

<success_criteria>
Plan 05-03 is complete when:
1. Both queue services exist, follow stage-rule-queue.service template line-for-line for connection + lifecycle
2. Summarizer runs concurrency=1, preserves previous summary on failure (D-21)
3. Retry queue enqueues with exponential backoff 2s/8s/32s and attempts=3 (D-19)
4. AGENT_PERSISTENT_FAILURE notification fires exactly once on final failed attempt
5. agent.module.ts registers both as providers AND exports (so runner in plan 05-04 can inject them)
6. Full specs green
</success_criteria>

<output>
After completion, create `.planning/phases/05-agent-conversation-flow/05-03-SUMMARY.md`
</output>
