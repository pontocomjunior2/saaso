---
phase: 2
slug: crm-v2-automa-o-de-r-guas
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-14
---

# Phase 2 — UI Design Contract: CRM v2 Automação de Réguas

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (custom Tailwind CSS v4) |
| Preset | not applicable |
| Component library | none (bespoke components, no shadcn/radix) |
| Icon library | lucide-react (already in use) |
| Font | Space Grotesk (primary), IBM Plex Mono (code/mono) |

Source: `apps/web/src/app/globals.css`, `apps/web/src/components/board/` scan.

No `components.json` found. Project uses a custom Tailwind v4 setup with inline CSS variables. No shadcn initialization needed — the existing pattern is bespoke and must be followed consistently.

---

## Spacing Scale

Declared values (multiples of 4 only):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline badge padding |
| sm | 8px | Compact element spacing, button icon gap |
| md | 16px | Default element spacing, card padding inner |
| lg | 20px | Card section padding (matches existing `p-5`) |
| xl | 24px | Sheet/modal header padding (matches `px-6 py-5`) |
| 2xl | 32px | Modal outer padding (matches `p-8`) |
| 3xl | 48px | Page-level section gaps |

Exceptions:
- Stage column width: fixed `21rem` (336px) — matches existing Kanban column.
- CardDetailSheet max-width: `34rem` (544px) — matches existing sheet.
- Touch targets (pause/resume toggle, takeover button): minimum `h-8 w-8` (32px) clickable area; preference `h-10 w-10` (40px) for primary takeover CTA.
- Autopilot toggle pill: `h-8 w-16` — matches existing toggle pattern in KanbanBoard.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px (text-sm) | 400 (regular) | 1.5 (leading-6) |
| Label / Meta | 11px | 700 (bold) | 1.4 — uppercase + tracking-[0.22em–0.32em] |
| Card title / Section heading | 15px | 700 (bold) | 1.3 (leading-snug) |
| Sheet heading / Modal title | 18–24px | 600 (semibold) | 1.2 |

Source: audited from `CardDetailSheet.tsx`, `StageTemplatesModal.tsx`, `KanbanBoard.tsx`.

Rules:
- ALL meta/label text uses uppercase + letter-spacing. Never use uppercase without tracking.
- Section headings inside sheets use `text-sm font-semibold text-slate-900`.
- Descriptive body inside sheet panels uses `text-sm leading-6 text-slate-500` or `text-slate-600`.
- Status badge text: 11px, font-medium, uppercase, tracking-[0.18em–0.28em].

---

## Color

All values sourced from `apps/web/src/app/globals.css` and audited component files.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#fafcfd` background / `#ffffff` surface | Page background, card surfaces, sheet background |
| Secondary (30%) | `#f0f0f0` line / `#f5f5f5` bg / `#f8f8fd` sidebar icon bg | Borders, muted containers, input backgrounds |
| Accent (10%) | `#594ded` | Specific interactive elements only (see list below) |
| Destructive | `#fc736f` (accent-danger) | Delete confirmations only |

Accent `#594ded` reserved for:
1. Primary action buttons ("Salvar", "Novo Pipeline", "Ativar Régua", "Iniciar Agente")
2. Active tab/pipeline underline indicator
3. Active stage column inline name input border + ring
4. Selected card ring (`ring-1 ring-[#594ded]`)
5. Pause/resume régua toggle in active state — use `#22bd73` (accent-success) for "running" state, `#d9d9d9` for paused
6. "Dashed add new" border on hover (template/régua add buttons)
7. Focus ring on all form inputs (`focus:border-[#594ded] focus:ring-1 focus:ring-[#594ded]`)

Semantic palette (in use, extend consistently):
| Color | Hex | Reserved for |
|-------|-----|--------------|
| Success / Autopilot ON | `#22bd73` | Agent autopilot toggle ON, emerald status badges |
| Warning / Takeover | `#f5bd4f` | Takeover required badge (amber tone) |
| Danger / Delete | `#fc736f` | Delete actions, error states, "Régua com erro" badge |
| Info / WhatsApp | `#3b78f5` | Email channel pill, "in-progress" stage dot |
| Accent soft bg | `#e8e6fc` / `#f5f3ff` | Form container bg when accent input is focused |
| Muted text | `#a0aec0` | Placeholder, secondary meta, empty state copy |
| Strong text | `#1a202c` | Card titles, modal headings, primary body |
| Body text | `#393939` | Main foreground (matches CSS var `--foreground`) |

Dark surface (configuracoes page pattern) — used for full-page settings panels:
- Background: `rgba(7,16,29,0.88)` / `rgba(8,18,34,0.82)`
- Border: `border-white/10`
- Body text: `text-slate-400`
- Heading text: `text-white`

The Meta Lead Ads configuration screen in `/configuracoes` MUST use this dark surface pattern to match the existing configuracoes page aesthetic.

---

## Component Inventory — New Components for Phase 2

All new components follow the bespoke pattern: no shadcn primitives, Tailwind utility classes only, lucide-react icons.

### 1. StageRuleDrawer (inline in stage gear menu)

Extends the existing `StageTemplatesModal` pattern. Opens as a full-screen overlay modal (`fixed inset-0 z-50`) triggered by gear icon on stage column header.

Tabs inside modal:
- "Templates" (existing — already implemented)
- "Régua" (new — D0, D+1, D+3, D+7 step builder)
- "Agente" (new — agent selector dropdown + classification criteria textarea)

Tab indicator: `border-b-2 border-[#594ded]` on active tab, matching pipeline tab pattern.

### 2. RuleStepList (inside Régua tab)

List of steps (D0, D+1, D+3...). Each step row:
- Left: day offset badge (`D0`, `D+1`) — `rounded-[6px] px-2 py-1 text-[10px] font-bold uppercase bg-[#f5f5f5] text-[#9e9e9e]`
- Center: template selector dropdown (select from stage templates)
- Right: delete step icon button `h-7 w-7 rounded-lg text-[#a0aec0] hover:bg-rose-50 hover:text-rose-500`

Add step: dashed border button at bottom — `border-dashed border-[#e2e8f0] hover:border-[#594ded] hover:bg-[#f5f3ff] text-[#594ded]`

### 3. AgentStepConfig (inside Agente tab)

- Agent selector: `<select>` styled matching existing channel select in StageTemplatesModal
- Classification criteria textarea: 4 rows, same `textarea` styling as body field in TemplateForm
- Helper text below: `text-[11px] text-[#a0aec0]` explaining criteria variables

### 4. CardRuleStatusPanel (inside CardDetailSheet)

Replaces/extends the existing "Status da régua" section. Adds:
- Pause/Resume toggle button inline with status label
- Toggle uses existing autopilot toggle pattern (`h-8 w-16 rounded-full`)
- "Pausada" state: `bg-[#d9d9d9]`; "Em execução" state: `bg-[#22bd73]`

### 5. AgentStatusBadge (inside CardDetailSheet)

Extends the existing "Agente desta etapa" card panel. Adds:
- Takeover button: `inline-flex items-center gap-2 rounded-xl border border-amber-200 bg-amber-50 px-3 py-2 text-sm font-medium text-amber-700 hover:bg-amber-100` (light mode)
- "Retomar Agente" button: `border-emerald-200 bg-emerald-50 text-emerald-700 hover:bg-emerald-100`
- Status text uses existing `conversationTone` mapping — no new color values

### 6. MetaWebhookConfigSection (in `/configuracoes` page)

New section added to existing configuracoes page. Uses the dark surface card pattern:
- Outer container: `rounded-[30px] border border-white/10 bg-[rgba(8,18,34,0.82)] p-6`
- List of form mappings: each row `rounded-[24px] border border-white/10 bg-white/[0.04] p-4`
- Add mapping button: `border border-white/10 bg-white/[0.06]` ghost style
- Meta Form ID input: uses same styling as other dark-mode inputs — `bg-white/[0.06] border-white/10 text-white`

---

## Interaction Contracts

### Régua pause/resume (CardDetailSheet)

- Button click → optimistic UI toggle (swap icon + label instantly)
- API call in background; on failure → revert state + show inline error text below button
- Busy state: show `<Loader2 className="h-3.5 w-3.5 animate-spin" />` inside button (matches existing `busyCardId` pattern)
- Transition: `transition` class only (no custom duration — matches existing card pattern)

### Agente takeover toggle (CardDetailSheet)

- Single click on "Assumir conversa" → POST to toggle endpoint
- Button disabled when `!activeConversation` (no conversation exists)
- Status badge updates immediately after success
- Uses existing `getConversationMeta()` + `conversationTone` mapping

### Stage rule/agent config (StageRuleDrawer modal tabs)

- Inline edit: clicking any step row puts it in edit mode (replaces row with form, same pattern as `StageTemplatesModal`)
- Save: `PATCH` call with optimistic list update
- Cancel: reverts to display row
- Tab switching: no API call — just renders different tab content
- Unsaved changes on tab switch: no blocking guard (keep it simple; tabs only switch config areas)

### Meta form mapping (configuracoes)

- "Adicionar mapeamento" opens inline form row (not a modal — same dashed-add pattern)
- Pipeline selector → Stage selector cascade (stage dropdown populates after pipeline selected)
- Meta Form ID: free-text input with placeholder "ex: 123456789"
- Save: POST call; success shows row in list; error shows inline error below form

### Drag-and-drop card movement (existing — régua cancellation side-effect)

- No UI change needed: cancellation of prior régua jobs is backend-only
- Card dragged to new stage shows existing stage-theme color during drag (already implemented)
- After drop: card badge updates from "Régua ativa" to "Régua na fila" (handled by `getRuleSummary()` via refresh)

---

## Copywriting Contract

| Element | Copy (Portuguese) |
|---------|-------------------|
| Primary CTA — régua config | "Salvar régua" |
| Primary CTA — agent config | "Salvar agente" |
| Primary CTA — pause régua | "Pausar régua" |
| Primary CTA — resume régua | "Retomar régua" |
| Primary CTA — takeover | "Assumir conversa" |
| Primary CTA — return to agent | "Devolver ao agente" |
| Primary CTA — Meta mapping | "Adicionar mapeamento" |
| Empty state — no rule steps | "Nenhuma etapa de régua configurada. Clique em + para adicionar o primeiro disparo." |
| Empty state — no agent assigned | "Nenhum agente atribuído a esta etapa." |
| Empty state — no Meta mappings | "Nenhum formulário Meta mapeado. Adicione um mapeamento para captar leads automaticamente." |
| Error — rule save failed | "Erro ao salvar régua. Verifique os campos e tente novamente." |
| Error — agent save failed | "Erro ao salvar configuração do agente." |
| Error — pause/resume failed | "Não foi possível alterar o status da régua. Tente novamente." |
| Error — takeover toggle failed | "Não foi possível alterar o status do agente. Tente novamente." |
| Error — Meta mapping save failed | "Erro ao salvar mapeamento. Verifique o ID do formulário Meta." |
| Destructive — delete rule step | "Remover etapa": "Esta etapa será removida da régua. Confirmar?" — inline confirm (replace delete icon with [Confirmar] [Cancelar] text buttons, no modal) |
| Destructive — delete Meta mapping | "Remover mapeamento": same inline confirm pattern, no modal |
| Status badge — régua running | "Régua em execução" |
| Status badge — régua paused | "Régua pausada" |
| Status badge — agent autopilot | "Piloto automático" |
| Status badge — agent takeover | "Takeover humano" |
| Status badge — handoff required | "Aguarda humano" |
| Activity log entry — agent moved card | "Agente [nome] moveu para [etapa] — [motivo]" |
| Activity log entry — rule step sent | "Régua D[n] disparada via [canal]" |
| Notification — new Meta lead | "Novo lead via Meta Ads entrou em [etapa]" |

---

## Page-level UI Surface Map

| Surface | Location | New UI Elements |
|---------|----------|----------------|
| Kanban stage column header | `KanbanBoard.tsx` — gear icon | Opens `StageRuleDrawer` (extends existing StageTemplatesModal with 3 tabs) |
| CardDetailSheet — agent panel | `CardDetailSheet.tsx` — "Agente desta etapa" section | Adds takeover/return button below agent name |
| CardDetailSheet — rule panel | `CardDetailSheet.tsx` — "Status da régua" section | Adds pause/resume toggle above step list |
| CardDetailSheet — activity | `CardDetailSheet.tsx` — ActivityTimeline | Shows "Agente [nome]" origin entries — no new component, data-driven |
| Configuracoes page | `apps/web/src/app/configuracoes/page.tsx` | New "Meta Lead Ads" dark card section at bottom |

No new top-level routes required. All Phase 2 UI is additive to existing surfaces.

---

## States Required Per Component

### CardRuleStatusPanel

| State | Visual |
|-------|--------|
| Running | Toggle ON (`bg-[#22bd73]`), badge "Régua em execução" |
| Paused | Toggle OFF (`bg-[#d9d9d9]`), badge "Régua pausada" |
| No run | Toggle disabled (opacity-50), text "Sem régua ativa" |
| Loading (toggle in-flight) | Loader2 icon inside toggle knob |
| Error | Inline error text below toggle, red (`text-rose-600`), no toast |

### AgentStatusBadge

| State | Visual |
|-------|--------|
| Autopilot (OPEN) | Emerald badge "Piloto automático", button "Assumir conversa" (amber) |
| Takeover (HANDOFF_REQUIRED) | Amber badge "Takeover humano", button "Devolver ao agente" (emerald) |
| No conversation | Slate badge "Sem conversa ativa", buttons hidden |
| Loading (toggle in-flight) | Button shows Loader2, disabled |
| Error | Inline error text, no toast |

### RuleStepList

| State | Visual |
|-------|--------|
| Empty | Dashed placeholder text — empty state copy above |
| Populated | List of step rows |
| Step editing | Row replaced by inline form (same TemplateForm pattern) |
| Step saving | Loader2 in save button |
| Step save error | Error banner above form row |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — no shadcn |
| third-party | none | not applicable |

No external component registries. All components are hand-built with Tailwind utilities following the existing codebase pattern.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
